name: C/C++ CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
#    - name: configure
#      run: ./configure
    - name: Install libncurses (term.h)
      run: sudo apt-get install libncurses5-dev
    - name: make
      run: make
#    - name: make check
#      run: make check
#    - name: make distcheck
#      run: make distcheck
